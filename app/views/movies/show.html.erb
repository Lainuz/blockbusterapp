<p style="color: green"><%= notice %></p>

<%# <%= render @movie %>

<p class="fs-2">Título: "<%= @movie.name %>"</p>

<div>
  <%= link_to "Editar película", edit_movie_path(@movie), class:"btn btn-warning btn-sm" %> |
  <%= link_to "Volver a lista de películas", movies_path, class:"btn btn-primary btn-sm" %>
</div>

<div class="d-flex my-3">
<%= image_tag ("cruz.svg") %>
<%= button_to "Eliminar película" , @movie, method: :delete, class:"btn btn-danger align-middle" %>  
</div>

